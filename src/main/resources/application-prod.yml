# Server Port
server:
  port: '8080'

spring:
  # Mysql
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/mungyeong?useSSL=false&serverTimezone=UTC&allowMultiQueries=true
    username: push
    password: tsei1234
   
# Actuator
management:
  endpoints:
    enabled-by-default: false